<head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p>The order from <%= @user.first_name %> <%= @user.last_name %> has still not been paid. Lets send them a reminder.Visit <%= link_to "Orders", orders_url %> to remind them to pay for their order.</p>
    <p>
    Order details<br>
    --------------------------
    </p>
    <p>Email: <%= @user.email %>
    </p>
    <p>
    Message:<br>
    ----------
    </p>
    <p>
        The Order:
        <table class = "table">
      <thead>
        <tr>
          <th>Order Id</th>
          <th>Name</th>
          <th>Status</th>
          <th>Total Cost</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <tr>
            <td><%= @order.id %></td>
            <td><%= @order.first_name %> <%= @order.last_name %></td>
            <td><%= @order.status%></td>
            <td><%= @order.subtotal%></td>
        </tr>
      </tbody>
    </table>
    <table class = "table">
        <thead>
            <tr>
              <th>Product</th>
              <th>Unit Price</th>
              <th>Size</th>
              <th>Quantity</th>
              <th>Item Total</th>
              <th colspan="3"></th>
            </tr>
        </thead>

        <tbody>
            <% @order_items.each do |item| %>
            <tr>
                <td><%= item.product.name %> </td>
                <td><%= item.product.price %> </td>
                <td><%= item.diffSize %></td>
                <td><%= item.quantity %> </td>
                <td><%= item.total %> </td>

            <% end %>
            </tr>
        </tbody>
    </table>
            


        <h2>Current Order Total: <%= @order.subtotal %> </h2>
    </p>
  </body>