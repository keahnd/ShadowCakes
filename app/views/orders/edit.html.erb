<% if user_signed_in? %>
	<% if current_user.admin? %>

		<h1>Editing Order</h1>

		<%= render 'form', order: @order %>

		<%= link_to 'Back', orders_path %>

	<%else%>

		<%= render 'pickup', order: @order %>

		<%= link_to 'Back', orders_path %>

	<%end%>

<% else %>

	<%= NOT AUTHORIZED TO VIEW THIS PAGE %>

<%end%>